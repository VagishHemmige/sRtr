% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_individual_coverage_gains.R
\name{rank_individual_coverage_gains}
\alias{rank_individual_coverage_gains}
\title{Rank individual candidate areas by marginal coverage gain}
\usage{
rank_individual_coverage_gains(
  candidate_areas,
  existing_areas = NULL,
  population_geography,
  coverage_vars,
  geography_id = NULL,
  method = c("intersection", "centroid", "areal"),
  crs = 5070
)
}
\arguments{
\item{candidate_areas}{An \code{sf} object containing candidate catchment
geometries to evaluate one at a time.}

\item{existing_areas}{An \code{sf} object containing the existing catchment
geometries, or \code{NULL} if there is no existing network.}

\item{population_geography}{An \code{sf} object containing the population or burden
geography to summarize. This object should contain the columns named in
\code{coverage_vars}.}

\item{coverage_vars}{Character vector of numeric column names in
\code{population_geography} to summarize.}

\item{geography_id}{Optional character string naming an identifier column in
\code{population_geography}. Passed to \code{\link[=calculate_geography_coverage_gain]{calculate_geography_coverage_gain()}}.}

\item{method}{Character string specifying how geographic coverage should be
calculated. One of \code{"intersection"}, \code{"centroid"}, or \code{"areal"}.}

\item{crs}{Coordinate reference system to use for spatial calculations.
Defaults to EPSG:5070.}
}
\value{
If \code{coverage_vars} has length 1, a tibble with one row per candidate
area. If \code{coverage_vars} has length greater than 1, a named list of tibbles
split by \code{coverage_var}. Each tibble includes the coverage summaries returned
by \code{\link[=calculate_geography_coverage_gain]{calculate_geography_coverage_gain()}}, the non-geometry columns from
\code{candidate_areas}, and a \code{rank} column ordered by descending
\code{newly_covered}.
}
\description{
\code{rank_individual_coverage_gains()} calculates the marginal coverage gain from
adding each candidate area to an existing coverage network one at a time.
}
\details{
This is a convenience wrapper around \code{\link[=calculate_geography_coverage_gain]{calculate_geography_coverage_gain()}}.
It is useful for ranking candidate catchment areas, such as buffers or
travel-time isochrones, by the population or burden newly covered after
accounting for overlap with an existing network.

The function assumes that \code{candidate_areas} and \code{existing_areas} already
contain the catchment geometries to evaluate. It does not create buffers,
travel-time isochrones, or other catchment areas internally.
}
