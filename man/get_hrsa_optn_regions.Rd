% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unos_regions_function.R
\name{get_hrsa_optn_regions}
\alias{get_hrsa_optn_regions}
\title{Get OPTN/UNOS Region Boundaries}
\usage{
get_hrsa_optn_regions()
}
\value{
An \code{sf} object with one feature per OPTN/UNOS region.
}
\description{
Returns OPTN/UNOS region boundaries from the package's internal
state-level UNOS region spatial dataset.
}
\details{
This function uses the package's internal \code{UNOS_regions_sf} object, which
contains state-level geometries assigned to OPTN/UNOS regions. The function
dissolves those state-level geometries into one feature per OPTN/UNOS region.

Unlike the HRSA ArcGIS \code{REGION_CD} and \code{REGION_NM} fields, which appear to
represent 10 HRSA/HHS administrative regions, this function returns the
11 OPTN/UNOS regions.

The returned object preserves the coordinate reference system of
\code{UNOS_regions_sf}, currently NAD83 / Conus Albers.
}
\examples{
\dontrun{
regions <- get_hrsa_optn_regions()

regions
sf::st_crs(regions)
plot(regions["Region"])
}

}
\seealso{
\code{\link[sf:valid]{sf::st_make_valid()}}, \code{\link[dplyr:summarise]{dplyr::summarise()}},
\code{\link[=get_hrsa_transplant_centers]{get_hrsa_transplant_centers()}}
}
